apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "libraries.fullname" . }}-test-connection"
  labels:
    {{- include "libraries.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "libraries.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
